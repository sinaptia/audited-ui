en:
  activerecord:
    attributes:
      audit:
        action: Action
        audited_changes: Changes
        comment: Comment
        created_at: Created at
        user: User
        version: Version
  audited:
    ui:
      audits:
        index:
          actions:
            create: Create
            update: Update
          after: After
          attribute: Attribute
          before: Before
          empty: There are no audit records yet.
          system: System
          title: Auditing %{what}
